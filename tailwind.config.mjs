/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS 4 uses CSS-first configuration.
  // All theme customizations are defined in src/styles/global.css via @theme.
  // This file exists for tooling compatibility.
};
